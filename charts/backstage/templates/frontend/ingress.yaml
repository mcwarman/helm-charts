{{- if .Values.frontend.ingress.enabled -}}
{{- $apiIsStable := eq (include "backstage.ingress.isStable" .) "true" -}}
{{- $ingressSupportsPathType := eq (include "backstage.ingress.supportsPathType" .) "true" -}}
{{- $serviceName := include "backstage.frontendFullname" . -}}
{{- $servicePort := .Values.frontend.service.port -}}
apiVersion: {{ include "backstage.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ $serviceName }}
  labels:
    {{- include "backstage.frontendLabels" . | nindent 4 }}
  {{- with .Values.frontend.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if and $apiIsStable .Values.frontend.ingress.ingressClassName }}
  ingressClassName: {{ .Values.frontend.ingress.ingressClassName }}
{{- end }}
  rules:
  {{- range .Values.frontend.ingress.hosts }}
    - host: {{ quote . }}
      http:
        paths:
          - path: /
            {{- if $ingressSupportsPathType }}
            pathType: Prefix
            {{- end }}
            backend:
            {{- if $apiIsStable }}
              service:
                name: {{ $serviceName }}
                port:
                  number: {{ $servicePort }}
            {{- else }}
              serviceName: {{ $serviceName }}
              servicePort: http
            {{- end }}
  {{- end }}
  {{- if .Values.frontend.ingress.tls }}
  tls:
  {{- range .Values.frontend.ingress.tls }}
    - hosts:
    {{- range .hosts }}
        - {{ quote . }}
    {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
  {{- end }}
{{- end }}
